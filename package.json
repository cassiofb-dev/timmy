{
  "name": "timmy",
  "version": "0.0.2",
  "description": "translator bot using openai services",
  "main": "src/timmy.js",
  "scripts": {
    "start": "node --env-file=.env src/timmy.js",
    "serve": "npm install && npm start",
    "deploy": "node --env-file=.env src/deploy-commands.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cassiofb-dev",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.20.0",
    "openai": "^5.6.0"
  }
}
